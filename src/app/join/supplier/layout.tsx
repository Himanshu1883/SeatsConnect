import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Become a Supply Partner — SeatsConnect™",
  description: "Apply to become a SeatsConnect supply partner and connect your inventory with approved B2B distribution channels.",
};

export default function JoinSupplierLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
