import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Become a Distribution Partner — SeatsConnect™",
  description: "Apply to become a SeatsConnect distribution partner and access global event supply through one B2B connection.",
};

export default function JoinPartnerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
