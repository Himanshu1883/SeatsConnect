export type PartnerLogo = {
  name: string;
  src: string;
  href?: string;
};

/**
 * Add approved logos here only after SeatsConnect has the commercial
 * relationship and permission to display them.
 */
export const partnerLogos: PartnerLogo[] = [
  // { name: "Approved Partner", src: "/logos/example.svg" },
];
